name: Deploy Pipelines - PROD

on:
  workflow_dispatch:
  push:
    branches:
      - EC-337-functions-cicd-pipelines
      
jobs:
  deploy_on_prod:
    uses: pagopa/dx/.github/workflows/legacy_deploy_pipelines.yaml@workflow-functions-test
    name: Deploy on PROD
    secrets: inherit
    with:
      environment: 'prod'
      resource_group_name: 'io-p-admin-rg'
      app_name: 'io-p-admin-fn'
      use_staging_slot: true
      use_private_agent: false
      health_check_path: '/info'
