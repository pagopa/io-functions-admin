describe("GetUsers", () => {
  it.todo(
    "should return an internal error response if the API management client returns an error"
  );

  it.todo(
    "should return an internal error response if the API management client returns invalid data"
  );

  it.todo("should return the user collection with the registered users");
});
